export enum TYPES {
  // Repositories

  // Services
  HashingService = "HashingService",
  JWTService = "JWTService",
  // Use Cases

  // Controllers

  // Middlewares
  ErrorHandlerMiddleware = "ErrorHandlerMiddleware",
}
